#pragma once

enum class Rotation {
    NONE = 0,
    SHORTEST = 1,
    ANTICLOCKWISE = 3,
    CLOCKWISE = 2
};

enum class Direction {
    NONE = 0,
    SHORTEST = 1,
    FORWARD = 2,
    BACKWARD = 3
};